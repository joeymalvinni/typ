# typ

terminal typing test

## plan

output buffer for the entire screen
raw + alternate mode for terminal (new screen), all input captured

optimized way to draw screen: only redraw what is necessary.
- focus on specific bounding boxes / parts of the screen (e.g. timer redraws every frame, can optimize by "page" of the app)


colored output

profile storage?

TODO: figure out typing behavior, what keybinds / what keys, is it configurable later, etc
